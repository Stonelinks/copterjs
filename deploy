#!/bin/bash

rsync -avz --rsh="ssh -i id_rsa" --exclude 'node_modules' . ssh -i id_rsa root@192.168.1.5:/root/copterjs
ssh -i id_rsa root@192.168.1.5 /bin/bash << EOF
cd /root/copterjs
npm i
if ps ax | grep -v grep | grep forever > /dev/null
then
  echo "forever is running, everything is fine"
else
  echo "forever is not running"
  forever start /root/copterjs/src/server/index.js
fi
EOF
